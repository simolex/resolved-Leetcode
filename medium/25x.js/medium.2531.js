/**
 * 2531. Make Number of Distinct Characters Equal
 *
 * @param {string} word1
 * @param {string} word2
 * @return {boolean}
 */
var isItPossible = function (word1, word2) {
    for (let c of word1) {
        console.log(c);
    }

    // const freqWord1 = word1.split("")
};

console.log(isItPossible("ac", "b"));
